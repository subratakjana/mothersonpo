import { TransformToUpperDirective } from './transform-to-upper.directive';

describe('TransformToUpperDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformToUpperDirective();
    expect(directive).toBeTruthy();
  });
});
