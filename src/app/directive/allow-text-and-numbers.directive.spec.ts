import { AllowTextAndNumbersDirective } from './allow-text-and-numbers.directive';

describe('AllowTextAndNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowTextAndNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
