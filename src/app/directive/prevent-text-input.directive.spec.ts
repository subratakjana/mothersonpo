import { PreventTextInputDirective } from './prevent-text-input.directive';

describe('PreventTextInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventTextInputDirective();
    expect(directive).toBeTruthy();
  });
});
