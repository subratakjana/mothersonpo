import { AllowOnlyTextDirective } from './allow-only-text.directive';

describe('AllowOnlyTextDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowOnlyTextDirective();
    expect(directive).toBeTruthy();
  });
});
